# MongoDB Configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your_secure_password_here
MONGO_DATABASE=silentium

# Application Ports
BACKEND_PORT=4000
FRONTEND_PORT=443

# Domain for SSL certificates
DOMAIN=yourdomain.com

# Optional: External MongoDB URI (if using external MongoDB)
# MONGODB_URI=mongodb://username:password@host:port/database?authSource=admin

# Other environment variables can be added as needed
# NODE_ENV=production (set automatically in docker-compose)
